[package]
name = "QuickUploader"
version = "0.1.0"
authors = ["David <david@templatious.org>"]

[dependencies]
safe_core = { version="0.3.0", features=["use-mock-routing"] }
safe_nfs = "0.3.0"
regex = "0.1.8"
regex_macros = "0.1.8"
